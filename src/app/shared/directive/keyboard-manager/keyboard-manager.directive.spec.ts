import { KeyboardManagerDirective } from './keyboard-manager.directive';

describe('KeyboardManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyboardManagerDirective();
    expect(directive).toBeTruthy();
  });
});
